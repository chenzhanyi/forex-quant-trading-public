# 外汇量化交易系统 — 改进建议

> 审查日期: 2026-07-06
> 范围: 全项目代码审查
> 原则: 只列出问题，不直接修改

---

## 一、🔴 可能导致运行错误的 Bug

### 1.1 缺失 `/api/history` 路由（前端 500 静默失败）

**位置:** `src/web/dashboard.py` + `src/web/templates/index.html:289`

前端 JavaScript 调用 `fetchJSON('/api/history')` 来展示"今日信号"卡片的统计数据，但 `dashboard.py` 中根本没有定义 `/api/history` 这个路由。请求返回 404，被 `catch(e) {}` 静默吞掉。这块卡片永远显示 `--`。

**建议:** 在 `dashboard.py` 中新增 `/api/history` 路由，返回 `SignalHistory().summary(days=1)`。或者如果暂时不需要，把前端的调用和卡片一起删掉。

---

### 1.2 `/api/entry` 端点传入原始数据给形态检测方法

**位置:** `src/web/dashboard.py:92-103` + `src/strategy/entry.py:71-112`

`api_entry()` 调用 `_prepare()` 后把 DataFrame 传给 `_detect_bullish_pattern(h1)` 和 `_detect_bearish_pattern(m15)` 等。`_prepare()` 只加了 SMA/EMA/ATR，但形态检测方法内部会访问 `close/open/high/low`，这些列没问题。不过 `_detect_bullish_pattern` 方法中有个参数 `df` 不包含 SMA/EMA 指标列（因为 `api_entry` 没有对这些调用前统一再补指标），当遇到 `m15` 的检测时光靠 `_prepare` M15 是 OK 的。

**实际风险比较低**（因为形态方法只使用 OHLC 原始数据），但 `is_consecutive_bear/bull` 被调用时依赖 `close` 和 `open` 列，不是 SMA/EMA，所以目前没有问题。这里列出主要是代码结构脆弱 — 如果以后形态检测加入指标依赖，这里就会炸。

**建议:** `api_entry` 中应该复用 `EntryDetector.detect()` 的完整流程，而不是手动调用内部 `_prepare` + `_detect_xxx_pattern`。

---

### 1.3 Config 单例每次都重新读取 YAML 文件

**位置:** `src/utils/config_loader.py:21-40`

`Config.load()` 每次被调用都重新 `yaml.safe_load()` 两个配置文件。项目中几乎所有模块都在 `__init__` 中调用 `config.load()`，虽然功能正确但每次实例化新对象就做一次磁盘 I/O。

**建议:** 在 `load()` 开头加 `if self._config: return self._config`。如果需要强制重载，加一个 `reload=True` 参数。

---

### 1.4 `news_collector.py` 全局修改 `os.environ` 代理变量

**位置:** `src/data_collection/news_collector.py:47-48`

```python
os.environ["http_proxy"] = proxy
os.environ["https_proxy"] = proxy
```

在 `NewsCollector.__init__()` 中直接修改全局环境变量，会影响同一进程中的所有 HTTP 请求。如果 Dashboard 和 NewsCollector 在同一个进程中运行（比如通过守护进程），代理设置会污染 OANDA 的直连请求。

**建议:** 改用 `httpx.Client(proxy=...)` 统一管理代理（和 `config_loader.py` 的 `get_http_client()` 设计保持一致）。`feedparser` 可以通过 `urllib` 的 `build_opener` 局部配置代理。

---

## 二、🟡 策略/逻辑层面的问题

### 2.1 置信度有两条独立的计算路径，互不对齐

**位置:** `src/strategy/signal.py:76-83` vs `src/strategy/entry.py:28, 181, 198`

`EntrySignal` 数据类有一个 `confidence` 字段（1-3 星），在 `EntryDetector` 中赋值：
- 金叉/死叉 → `confidence=2`
- 反转入场 → `confidence=min(5, int(score))`

但是 `SignalGenerator.generate()` 第 76-83 行**完全忽略**了 `entry.confidence`，从基础分 3 重新计算 `FinalSignal.confidence`。这意味着：
- EntryDetector 花了心思评的分 → 被丢掉
- SignalGenerator 自己评的分 → 和 EntryDetector 评分逻辑没有继承关系
- 反转入场的置信度评分（基于 K 线形态权重）在 SignalGenerator 层面完全看不到

**建议:** 让 `SignalGenerator.generate()` 以 `entry.confidence` 为基础分（而不是硬编码 `conf=3`），再叠加趋势一致性和 M15 确认的加分。

---

### 2.2 `_detect_buy()` 中有死代码

**位置:** `src/strategy/entry.py:161-165`

```python
h1_aligned = (h1_last[self.sma_col] > h1_last[self.ema_col] and
              self.ind.is_consecutive_bull(
                  h1[[self.sma_col]].rename(columns={self.sma_col: "close"}) if False else h1,
                  2) is False)
```

这段代码问题：
1. `if False else h1` — `False` 分支永远不执行，等于直接传 `h1` 给 `is_consecutive_bull`
2. 如果 `False` 为真，`h1[[self.sma_col]]` 只选了一列 SMA，rename 成 `close` 后 DataFrame 没有 `open` 列 — `is_consecutive_bull` 访问 `open` 列会炸
3. `h1_aligned` 变量被赋值后**从未被使用**（后面的实际逻辑用的是 `sma_up` 和 `ema_up` 斜率）

**建议:** 删除这段死代码，或者重构为可读的实现。当前正确工作的其实是第 167-182 行的斜率判断逻辑。

---

### 2.3 `_check_slope_momentum` 被重复调用，加载相同数据

**位置:** `src/strategy/entry.py:114-135`

`_detect_buy()` 在第 158 行调用 `_check_slope_momentum("BUY")`，这个方法从磁盘/API 加载 H4 数据，计算 SMA，再算斜率。如果做多检测失败后去做空检测，`_detect_sell()` 在第 222 行再次调用 `_check_slope_momentum("SELL")`，又加载一遍完全相同的 H4 数据。

虽然实际上每次 `detect()` 只调用一个方向（做多或做空），不是两个都调用——但这个方法本身就独立于方向（参数 `direction` 没有被使用！），方法签名是 `_check_slope_momentum(self, direction: str)` 但 `direction` 在函数体内完全没有被引用。

**建议:** 
1. 删除无用的 `direction` 参数
2. 在 `detect()` 中只调用一次，把结果传给 `_detect_buy/_detect_sell`

---

### 2.4 风控模块数据加载失败时静默回退到硬编码 20 pips 止损

**位置:** `src/strategy/risk.py:126-127, 168-179`

```python
def _widest_stop(self, direction, entry, h1, m15):
    if h1 is None or h1.empty:
        return (entry - 0.0020, "默认") if direction == "BUY" else (entry + 0.0020, "默认")
```

当 H1 数据无法加载时（`_load_data` 静默吞掉所有异常），止损直接设为 20 pips（0.0020），标签"默认"。用户看到止损 = 20 pips 时无法区分这是系统计算出来的还是"数据坏了兜底猜的"。

**建议:** 
1. 数据加载失败时应该打 WARNING 级别的 log
2. 硬编码止损应该在 config 中可配置
3. `_load_data` 不应该静默吞异常

---

### 2.5 `trend.py` 类 docstring 和实际配置不一致

**位置:** `src/strategy/trend.py:18-19`

Docstring 写的是 "D1 50 SMA + H4 50 SMA"，但 config 和实际代码用的是 H4 **200** SMA。这在项目说明中也多次提到 200 SMA 是"经典多周期共振组合"。Docstring 过期没更新。

**建议:** 修正 docstring。

---

### 2.6 趋势判断中 `BULLISH_CAUTIOUS` / `BEARISH_CAUTIOUS` 状态在置信度计算中被忽略

**位置:** `src/strategy/trend.py:85-92` vs `src/strategy/signal.py:79`

趋势分析器在 D1 看多 + H4 横盘时返回 `overall="BULLISH_CAUTIOUS"`，且 `can_trade=True`。但在信号生成器第 79 行：
```python
if trend["overall"] in ("BULLISH", "BEARISH"):
    conf += 1
```
`"BULLISH_CAUTIOUS"` 不在这两个值之中，所以**谨慎看多时不会获得趋势加分**。这是合理的保守设计，但如果是有意的，应该在注释中说明。

**建议:** 加注释说明 `BULLISH_CAUTIOUS` 不参与置信度加分是设计意图。

---

## 三、🟠 数据完整性与风险

### 3.1 项目说明中的 H4 SMA 多处写成 "50 SMA"

**位置:** `项目说明.md` 多处

项目说明中很多地方仍然写 "H4 50SMA"（如第五节信号格式、第六节晚间简报格式），但实际配置和代码用的是 H4 **200** SMA。这是前期版本遗留。

**建议:** 全局搜索替换文档中的 "H4 50SMA" → "H4 200SMA"。

---

### 3.2 `ECBData` 数据源可能已不可用

**位置:** `src/data_collection/ecb_data.py`

ECB Data Portal (data-api.ecb.europa.eu) 已于 2025 年停止服务，现在 ECB 的数据迁移到了新的 SDMX 2.1 API。当前代码可能在运行时持续报错（被上层 catch 吞掉）。

**建议:** 更新 ECB 数据源到新的 API 端点，或者直接从 OANDA 获取足够的汇率数据。

---

### 3.3 Parquet 加载缺少数据完整性校验

**位置:** `src/data_collection/oanda.py:147-157` 和各处 `load_parquet` 调用

`load_parquet()` 加载数据后不做任何校验：
- 不检查必需的列是否存在 (`open/high/low/close/volume`)
- 不检查是否有 NaN 值
- 不检查数据是否过期（比如最后一条 K 线是 3 天前的）
- 不检查是否有异常价格（比如 EUR/USD = 0.5 或 2.0）

**建议:** 添加 `validate_data(df)` 方法，至少检查列完整性和最后更新时间。

---

### 3.4 `config.local.yaml` 中 OANDA API Key 被提交的风险

**位置:** `config/config.local.yaml`

`.gitignore` 中没有明确排除 `.local.yaml` 文件。如果这个文件被 push 到公开仓库，OANDA 实盘/模拟盘的 API Key 就泄露了。

**建议:** 确认 `.gitignore` 中有 `*.local.yaml` 或 `config.local.yaml`。

---

## 四、🟢 Dashboard 前端问题

### 4.1 `debug=True` 在生产环境不安全

**位置:** `src/web/dashboard.py:251`

```python
app.run(host="127.0.0.1", port=5001, debug=True)
```

`debug=True` 会启用 Werkzeug 调试器和自动重载。虽然绑定的是 `127.0.0.1`，但如果通过 SSH 端口转发暴露出去，调试控制台可以被远程访问。

**建议:** 从环境变量读取 debug 开关，默认 `False`。

---

### 4.2 前端错误处理全部静默

**位置:** `src/web/templates/index.html` 多个 `catch(e) {}`

所有 fetch 失败时，用户看不到任何错误提示。比如 `/api/signal` 挂了，页面就永远显示"加载中..."或上一次的数据，用户完全不知道出问题了。

**建议:** catch 块中至少更新对应 DOM 元素显示"加载失败"。

---

### 4.3 历史信号数据源混用了 JSONL 和 Markdown

**位置:** `src/web/dashboard.py` 和 `src/daemon/history.py` vs `output/signals/*.md`

- 守护进程用 `SignalHistory` 写 `data/signal_history.jsonl`（结构化 JSON）
- 信号生成器用 `SignalGenerator.save()` 写 `output/signals/*.md`（Markdown 格式）
- 复盘引擎从 Markdown 文件中解析信号（用正则）
- Dashboard 前端想从 `/api/history` 读（但路由不存在）

四个地方读写信号用了三种不同的路径和格式。Markdown 是给人看的，JSONL 是给程序读的，但它们没有同步机制。

**建议:** 每次生成信号时（`SignalGenerator.generate()`），同时写 Markdown（给人看）和 JSONL（给程序读）。复盘引擎优先读 JSONL，Markdown 仅作降级备份。

---

### 4.4 "今日信号"卡片设计语义不清

**位置:** `src/web/templates/index.html:145-149`

```html
<div class="card">
    <div class="card-title">📊 今日信号</div>
    <div id="todaySignalCount" class="value">--</div>
    <div id="todaySignalDetail" class="label">--</div>
</div>
```

这个卡片尝试展示"今天生成了多少次信号"，但紧挨着"交易建议"卡片（展示当前最新信号），两个卡片概念不同却都在 signal grid 中。给用户的认知负担是："今日信号 0 次"但旁边"交易建议"显示"做多"，这两者什么关系？

**建议:** 要么让这个卡片展示"最近 N 次信号的命中率"（和复盘数据联动），要么删掉它，把位置让给更直观的指标（如"当前持仓/挂单状态"）。

---

## 五、🔵 可维护性 / 工程质量

### 5.1 零测试覆盖

**位置:** 全项目

没有任何单元测试或集成测试文件。`demo_data.py` 能生成模拟数据，但没有被任何测试代码消费。策略逻辑的正确性完全依赖手工验证。

**建议:** 至少为核心模块添加测试：
- `Indicators` 类的 SMA/EMA/ATR 计算（可以用固定输入验证输出）
- `TrendAnalyzer` 的趋势判断矩阵
- `RiskManager` 的止损计算
- 用 `demo_data.py` 生成的模拟数据跑端到端信号生成

---

### 5.2 `requirements.txt` 包含未使用的依赖

**位置:** `requirements.txt`

以下包被列出但代码中没有任何 `import` 引用：

| 包 | 说明 |
|---|---|
| `pandas-ta` | 项目用纯 pandas 实现了所有指标 |
| `schedule` | 项目用系统 cron + 守护线程 |
| `fredapi` | FRED API 尚未集成 |
| `textblob` | 未使用（情绪分析用的是关键词匹配） |
| `mplfinance` | 未使用（没有图表生成） |
| `matplotlib` | 未使用 |

累计约 50MB 的额外依赖。

**建议:** 清理未使用的依赖。如果 FRED / 图表 是规划中的功能，挪到单独的 `requirements-dev.txt`。

---

### 5.3 Dashboard 路由函数内重复 import

**位置:** `src/web/dashboard.py:37-39`

```python
from src.analysis.fundamentals import FundamentalsAnalyzer
import json as jlib
from pathlib import Path as PPath
```

`FundamentalsAnalyzer` 已在文件顶部 import（第 12 行）。`json` 和 `Path` 也在顶部 import 过（但用的是别名 `jlib` 和 `PPath`）。虽然 Python 会缓存 import，但这是代码坏味道。

**建议:** 删除函数内的重复 import。

---

### 5.4 缺少类型注解

**位置:** 全项目

虽然 `entry.py` 和 `signal.py` 用了 dataclass 和类型注解，但 `trend.py`、`risk.py`、`indicators.py` 中很多方法的返回值是裸 `Dict`。比如 `TrendAnalyzer.analyze() -> Dict`，调用方需要阅读源码才知道返回值的键名。

**建议:** 为核心数据流添加 TypedDict：
```python
from typing import TypedDict

class TrendResult(TypedDict):
    D1: dict
    H4: dict
    overall: str
    can_trade: bool
    reason: str
```

---

### 5.5 日志级别不统一

**位置:** 全项目

- `src/daemon/engine.py:12` 在模块级别 `logging.basicConfig(level=logging.INFO)` — 导入这个模块就会改变全局日志配置
- `src/web/dashboard.py:17` 也有自己的 `basicConfig`
- `src/strategy/signal.py` 只 `getLogger` 但不设置 level

**建议:** 日志配置统一到一个地方（比如守护进程入口和 Dashboard 入口各自初始化一次），模块内部只 `getLogger`。

---

## 六、📊 问题优先级汇总

| 优先级 | 编号 | 问题 | 影响 |
|--------|------|------|------|
| 🔴 P0 | 1.1 | 缺失 `/api/history` 路由 | 前端卡片静默失效 |
| 🔴 P0 | 1.2 | `/api/entry` 调用方式脆弱 | 潜在运行时异常 |
| 🔴 P0 | 2.1 | 置信度两条计算路径不对齐 | 信号质量评分失真 |
| 🔴 P0 | 2.4 | 风控数据失败静默回退 | 止损不准确 |
| 🟡 P1 | 1.3 | Config 重复读磁盘 | 性能浪费 |
| 🟡 P1 | 1.4 | 全局代理变量污染 | 网络请求异常 |
| 🟡 P1 | 2.2 | `_detect_buy` 死代码 | 代码可读性 |
| 🟡 P1 | 3.3 | Parquet 无数据校验 | 脏数据静默进入系统 |
| 🟡 P1 | 4.3 | 信号存储格式不统一 | 复盘数据源混乱 |
| 🟠 P2 | 2.3 | 斜率检查重复调用 | 性能浪费 |
| 🟠 P2 | 2.5 | Docstring 过时 | 误导 |
| 🟠 P2 | 3.1 | 文档中 SMA 数值不一致 | 概念混淆 |
| 🟠 P2 | 3.4 | API Key 泄漏风险 | 安全 |
| 🟠 P2 | 5.2 | 未使用依赖过多 | 安装慢 / 依赖冲突风险 |
| 🟢 P3 | 4.1 | `debug=True` | 安全问题（小范围） |
| 🟢 P3 | 4.2 | 前端错误静默 | UX 不佳 |
| 🟢 P3 | 4.4 | "今日信号"卡片语义不清 | UX 困惑 |
| 🟢 P3 | 5.1 | 零测试覆盖 | 长期维护困难 |
| 🟢 P3 | 5.3 | 重复 import | 代码整洁 |
| 🟢 P3 | 5.4 | 缺少类型注解 | IDE 支持弱 |
| 🟢 P3 | 5.5 | 日志配置不统一 | 排查困难 |

---

## 七、建议的修改顺序

如果要分阶段改，建议的顺序：

1. **第一阶段（紧急修复）:** 1.1 → 2.1 → 2.4 → 1.4
2. **第二阶段（策略完善）:** 2.2 → 2.3 → 3.3 → 4.3
3. **第三阶段（工程质量）:** 5.1 → 5.2 → 5.5 → 3.1
4. **第四阶段（体验优化）:** 4.1 → 4.2 → 4.4 → 5.3 → 5.4
