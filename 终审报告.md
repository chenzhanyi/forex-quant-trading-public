# 外汇量化交易系统 — 终审报告

> 审计时间: 2026-07-06
> 范围: 全部 23 个源文件的逐行审查 + 跨模块一致性检查
> 方法: 代码逻辑分析 + 配置引用追踪 + 前后端数据流核对

---

## 🔴 关键问题 (P0)

### P0-1: Config 结构不匹配 — LLM 配置读取路径错误

**位置:** `config/config.yaml:76` vs `src/utils/dashboard_settings.py:48`

```yaml
# config.yaml — LLM 在 strategy 下
strategy:
  llm:
    cli_command: "claude-doubao"
    cli_args: ["-p", "--dangerously-skip-permissions", "--print", "--output-format", "json"]
```

```python
# dashboard_settings.py:48 — 在根级别查找
yaml_cfg = config.load().get("llm", {})  # ❌ 永远返回 {}
```

**后果:** YAML 中的 LLM 默认配置被彻底忽略。面板设置的 `dashboard_settings.json` 成为唯一数据源。如果该文件不存在或损坏，LLM 模块会用代码中的硬编码默认值。

**修复:** `dashboard_settings.py:48` 改为 `config.load().get("strategy", {}).get("llm", {})`，或者把 `llm` 移到 YAML 根级别。

---

### P0-2: 守护进程在午夜会崩溃

**位置:** `src/daemon/engine.py:96`

```python
target = now.replace(hour=current_hour + 1, minute=target_min, second=0)
```

当 `current_hour == 23` 时，`current_hour + 1 = 24`，`datetime.replace(hour=24)` 抛出 `ValueError`。信号线程在每天 23:05 之后崩溃，守护进程静默停止。

**修复:** 用 `now + timedelta(hours=1)` 替代手动 hour 计算，或对 24 取模。

---

### P0-3: 复盘引擎 TP1/TP2 正则匹配不到字段

**位置:** `src/strategy/review.py:143-144` vs `src/strategy/signal.py:164-165`

```python
# review.py — 正则找 "目标 1" 和 "目标 2"
m = re.search(r"目标 1[：:]\s*([\d.]+)", line)   # 匹配 "目标 1: 1.09000"
m = re.search(r"目标 2[：:]\s*([\d.]+)", line)   # 匹配 "目标 2: 1.09500"
```

```python
# signal.py — 实际上输出的是 "TP1" 和 "TP2"
f"🎯 TP1: {signal.take_profit_1:.5f} (+{tp1_pips:.0f} pips) [50%]\n"
f"🎯 TP2: {signal.take_profit_2:.5f} (+{tp2_pips:.0f} pips) [50%]\n"
```

**后果:** 复盘引擎在 Markdown 降级解析路径中，stop_loss / take_profit_1 / take_profit_2 永远为 `None`。这意味着复盘报告的盈亏计算（MAE/MFE/HIT判定）数据不完整。

**修复:** 统一两处的字段名——要么 review.py 改匹配 `TP[12]`，要么 signal.py 的格式化改回中文名称。

---

### P0-4: `analyze_sentiment` 不按日期过滤数据

**位置:** `src/analysis/fundamentals.py:232-250`

```python
def analyze_sentiment(events, days=7):
    from datetime import datetime, timedelta
    cutoff = datetime.now() - timedelta(days=days)  # ← 计算了但从未使用！
    for e in events:                                 # ← 遍历全部事件，不检查日期
        ...
```

**后果:** 函数接受 `days` 参数声称"分析最近 N 天"，实际统计了所有历史事件。基本面情绪评分的时效性完全丢失——3 个月前的数据和昨天的数据权重相同。

**修复:** 在循环内加 `if event_timestamp < cutoff: continue`。

---

## 🟡 高优先级 (P1)

### P1-5: `is_consecutive_bear` / `is_consecutive_bull` 索引逻辑反了

**位置:** `src/analysis/indicators.py:156-157`

```python
# 注释: "close[-2] (older) > close[-1] (newer) → 价格下降"
# 实际代码:
if i > 0 and df["close"].iloc[idx] <= df["close"].iloc[idx + 1]:
    return False
```

当 `i=1, idx=-2`: 比较 `close[-2] <= close[-1]` → 当较旧的收盘价 ≤ 较新的收盘价时为 True → 返回 False（判定不是连续下跌）。但「较旧的收盘价 ≤ 较新的收盘价」意味着价格在**上涨**。逻辑应该用 `>=` 来判定连续下跌（较旧 > 较新 = 价格下跌）。

**后果:** `is_consecutive_bear` 实际在检查「价格连续上涨」而非「连续下跌」，`is_consecutive_bull` 同理反向。这会影响入场检测中反转入场的条件判断。但由于反转条件中还有形态检测兜底，实际影响有所缓解。

---

### P1-6: 15 个死配置键 — 修改这些配置不会有任何效果

| 死配置键 | 位置 | 说明 |
|----------|------|------|
| `strategy.entry.max_distance_from_h4_sma` | config.yaml:64 | entry.py 硬编码 1.5，不读配置 |
| `strategy.entry.fib.primary_level` | config.yaml:69 | 硬编码 50%，不读配置 |
| `strategy.volatility.daily_range_pips` | config.yaml:67 | 整个 volatility 节无人使用 |
| `strategy.volatility.stop_loss_*` | config.yaml:68-70 | 同上 |
| `strategy.risk.position_pct` | config.yaml:74 | 未使用 |
| `strategy.risk.max_consecutive_loss` | config.yaml:83 | 连续亏损冷却未实现 |
| `strategy.risk.cooldown_hours` | config.yaml:84 | 同上 |
| `strategy.trading_style` | config.yaml:87 | 未使用 |
| `strategy.max_signals_per_week` | config.yaml:88 | 未使用 |
| `collection.news_interval_minutes` | config.yaml:123 | 未使用 |
| `account.max_risk_percent` | config.yaml:34 | 未使用（risk.py 用 risk_per_trade） |
| `fred.api_key` | config.yaml:21 | FRED 模块不存在 |
| `output.logs_dir` | config.yaml:131 | 多个模块用自己的路径 |

**后果:** 用户修改这些配置键完全无效，造成困惑。建议：要么实现对应功能，要么从 config 中删除。

---

### P1-7: 项目说明与代码实现的重大偏差

| 文档描述 | 代码实际 |
|----------|---------|
| "D1 50 SMA + H4 **50** SMA" (多处) | H4 用 **200** SMA |
| "连续亏损 3 次 → 停 24h" (§8) | 未实现，config 键是死的 |
| "晚间简报：挂单状态+持仓回顾+深夜预警" (§6) | 未实现，daemon 只生成信号和复盘 |
| "周五检查：有数据→撤单，无数据→正常" (§4) | 代码只是全周五不交易 |
| ECB 数据源存在 | ecb_data.py 实现完好但无模块调用 |

---

### P1-8: 复盘引擎 `get_price_window` 忽略小时精度

**位置:** `src/strategy/review.py:181-183`

```python
mask = (df.index >= start.strftime("%Y-%m-%d")) & (df.index <= end.strftime("%Y-%m-%d"))
```

`strftime("%Y-%m-%d")` 将 datetime 截断为日期字符串。`hours_before` 和 `hours_after` 参数被接受但实际无效——所有窗口都是全天级别的。复盘时无法做到"信号后 48 小时"的精确评估。

---

## 🟠 中优先级 (P2)

### P2-9: `find_swing_points` 冗余逻辑 — 高点永远不往前找

`indicators.py:392` — 当 swing 高点/低点太近时，`last_high` 被赋值为 `swing_highs[-1]`（本身），从不往前找更早的高点。只改变了 `last_low`。

### P2-10: dashboard.py 中 LLM 情绪 source 字段被覆盖

当 LLM 返回 fallback 结果 (`source: "fallback"`) 时，dashboard.py line 53 无条件覆盖为 `"Claude LLM"`。用户无法区分真正的 AI 分析和降级数据。

### P2-11: Dashboard refresh 跳过 M5 周期

`dashboard.py:276` 只刷新 `["D1", "H4", "H1", "M15"]`，M5 被跳过。

### P2-12: `_widest_stop` 的方法检测 bug

`risk.py:174` 用 `abs(sl - ema15)` 而非 `abs(sl - sl_e)` 来判断方法。EMA 止损方法判定可能不准确。

### P2-13: `evaluate_signal` 中 entry_found 过于宽松

`review.py:253` BUY 方向的 `entry_found = min_price <= entry_high` — 只要最低价低于入场区就判定为"入场触发"，不检查价格是否实际进入区间。

### P2-14: `parse_event_datetime` 对 `%b%d` 格式的日期解析为 1900 年

`calendar.py:164` — `strptime("Jul06", "%b%d")` 返回 `datetime(1900, 7, 6)`。后续与 `datetime.now()` 比较时永远判定为过去。ForexFactory HTML 降级路径的重大事件检查始终返回 False。

---

## 🟢 低优先级 (P3)

### 未使用的函数 / 代码

| 模块 | 函数/变量 | 说明 |
|------|----------|------|
| `indicators.py` | `detect_cross()` | 从无调用 |
| `indicators.py` | `from typing import List, Optional` | 导入未使用 |
| `calendar.py` | `has_upcoming_high_impact()` | 被 `has_major_event_soon()` 替代 |
| `ecb_data.py` | `fetch_exchange_rate()` & `collect_and_save()` | 整个模块无人调用 |
| `engine.py` | `from datetime import timezone` | 导入未使用 |
| `index.html` | `renderTag()` | 定义后从未调用 |
| `trend.py` | `format_one_liner()` | 定义后从未调用 |
| `fundamentals.py` | `collect_and_classify()` | 仅 `__main__` 中可用 |

### 健壮性 / 体验问题

- **Config 线程安全:** Singleton `Config.__new__` 无锁，多线程下可能创建多个实例
- **signal_history.jsonl 无限增长:** 无保留策略，数月后文件可到数 MB
- **`_seconds_until_h1_closed` 累积漂移:** 每轮的处理时间叠加导致下次延迟
- **前端 `refreshData` 不检查刷新是否成功即重载数据**
- **前端 Auto-refresh 只刷新信号，不刷新基本面/日历/状态**
- **`loadReview` 无超时处理，网络故障时永久显示"加载中"**

---

## 📊 修复优先级建议

| 优先级 | 编号 | 问题 | 预计改动量 |
|--------|------|------|-----------|
| 🔴 | P0-1 | Config LLM 路径不匹配 | 1 行 |
| 🔴 | P0-2 | Daemon 午夜崩溃 | 3 行 |
| 🔴 | P0-3 | Review TP 正则不匹配 | 2 行 |
| 🔴 | P0-4 | 情绪分析不按日期过滤 | 3 行 |
| 🟡 | P1-5 | is_consecutive_bear/bull 反向 | 2 行 |
| 🟡 | P1-6 | 死配置键清理 | 删除 ~15行 |
| 🟡 | P1-8 | get_price_window 精度 | 2 行 |
| 🟠 | P2-9 | swing_points 冗余逻辑 | 2 行 |
| 🟠 | P2-10 | LLM source 覆盖 | 3 行 |
| 🟠 | P2-11 | M5 缺失 refresh | 1 行 |
| 🟠 | P2-12 | _widest_stop 方法检测 | 1 行 |
| 🟠 | P2-14 | parse_event_datetime 1900 年 | 5 行 |

**建议修改顺序:** P0-1 → P0-2 → P0-3 → P0-4 → P1-5 → P1-6 → P1-8 → 剩余 P2

---

*审计范围: 23 个文件，发现了 22 个实质性问题（4 个关键，4 个高优，6 个中优，8 个低优）*
